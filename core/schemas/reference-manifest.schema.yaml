type: object
required: [reference_id, owner, role, grade]
properties:
  reference_id: {type: string}
  owner: {enum: [BRIDE, GROOM, COUPLE, SCENE, PROP, POSE, VIDEO, AUDIO, UNRESOLVED]}
  role: {type: string}
  grade: {enum: [S, A, B, C, D]}
  allowed_for_generation: {type: boolean}
  controls: {type: array}
  must_not_transfer: {type: array}
